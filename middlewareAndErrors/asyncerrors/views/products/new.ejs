<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Product</title>
</head>
<body>
    <h1><%= farm.name %></h1>
    <h2>Add a Product</h2>
    <form action="/farms/<%= farm._id %>/products" method="POST">
        <label for="name">Product Name:</label> 
        <input type="text" name="name" placeholder="Name of the product" id="name">
        
        <label for="price">Product Price(Unit):</label> 
        <input type="number" name="price" placeholder="Price" id="price">
        
        <label for="category">Select category</label>
        <select name="category" id="category">
            <% for (let category of categories){%>
                <option value="<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <br><br>
        <button>Submit</button>
    </form>
    <a href="/farms/<%= farm._id %>">Back to farm</a>
</body>
</html>